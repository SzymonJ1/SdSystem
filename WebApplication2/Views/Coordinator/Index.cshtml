@model IEnumerable<SdSystem.Models.Ticket>

<h2>Lista Zgłoszeń</h2>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Tytuł</th>
            <th>Opis</th>
            <th>Priorytet</th>
            <th>Status</th>
            <th>Utworzone przez</th>
            <th>Przydzielone do</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var ticket in Model)
        {
            <tr>
                <td>@ticket.TicketId</td>
                <td>@ticket.Title</td>
                <td>@ticket.Description</td>
                <td>@ticket.Priority</td>
                <td>@ticket.Status</td>
                <td>@ticket.CreatedBy?.Username</td>
                <td>@ticket.AssignedTo?.Username</td>
                <td>
                    <a asp-action="Assign" asp-route-id="@ticket.TicketId">Przydziel</a> |
                    <a asp-action="SetPriority" asp-route-id="@ticket.TicketId">Ustaw priorytet</a>
                </td>
            </tr>
        }
    </tbody>
</table>
